[pytest]
testpaths = tests
# conftest.py lives in tests/ and is imported directly by the test modules.
pythonpath = tests
addopts = -q --strict-markers
filterwarnings =
    error::DeprecationWarning:lambdas.*
